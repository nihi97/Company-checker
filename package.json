{
  "name": "company-checker",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "vite": "^4.0.0",
    "gh-pages": "^3.2.3",
    "@vitejs/plugin-react": "^4.2.0"  // Added this line
  },
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"  // Changed 'build' to 'dist' because that's the default Vite output folder
  },
  "homepage": "https://nihi97.github.io/Company-checker",
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  }
}
